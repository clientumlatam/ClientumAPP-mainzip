Conversación con Gemini

# Inventario de secretos y configuraciones por módulo


> Documento de referencia para configurar ClientumCRM de forma segura.

>

> **Importante:** este archivo contiene nombres de variables y ejemplos de

> formato, nunca valores reales. Los secretos deben guardarse en **Replit

> Secrets** o en una integración OAuth/API administrada; no deben escribirse

> en el código, en `localStorage`, en commits ni en este documento.


## Estado actual del proyecto


### Regla de separación


- **Secretos de la plataforma:** viven en Replit Secrets o en variables de

entorno del backend. Son compartidos por la aplicación completa y nunca se

cargan en el tab de API Keys de un usuario.

- **API Keys de usuario:** se crean y revocan desde

`Configuración → Integraciones & API Hub → API Keys por usuario`. Pertenecen

a un `user_id`, tienen scopes de módulos y no son variables de entorno.

- **Token de API:** el valor completo se muestra una sola vez al generarlo.

La interfaz no lo persiste en `localStorage`; en producción debe guardarse

únicamente su hash en el backend.


| Variable | Estado | Uso actual |

| --- | --- | --- |

| `GEMINI_API_KEY` | **No configurada** | Es la única clave que el backend consume actualmente. Habilita Copilot, CMO, GTM, Ad Copy, prospección y agentes IA. |

| `SESSION_SECRET` | **Disponible** | Está registrada en Replit Secrets, pero el servidor actual no usa sesiones Express. Reservarla para una futura sesión server-side. |

| `VITE_FIREBASE_*` | **No configuradas como Secrets** | Son configuración cliente de Firebase. El código actual tiene valores de fallback; para producción deben definirse como variables `VITE_` y quitar los fallbacks hardcodeados. |

| `DATABASE_URL` | **Administrada por Replit** | No debe solicitarse ni configurarse manualmente. La aplicación actual no consume PostgreSQL directamente. |


## Reglas de seguridad


1. Nunca pegues una API key, token, contraseña, certificado o clave privada

en el chat o en un archivo `.md`.

2. Las variables `VITE_*` llegan al navegador. Solo deben contener

configuración pública o claves restringidas por dominio; nunca tokens

privados, secretos de webhook o claves de administración.

3. Todo token de servidor debe leerse desde `process.env` y usarse únicamente

en endpoints backend.

4. Los webhooks deben validar firma y timestamp antes de procesar eventos.

5. Las credenciales de terceros deben preferirse mediante una integración

OAuth/API administrada cuando esté disponible.

6. Para producción conviene usar valores separados de desarrollo y producción.

7. Si un proveedor permite permisos por alcance, crear una credencial con el

mínimo alcance necesario.


---


## 1. Gestión comercial


### Resumen Ejecutivo


**No requiere secretos adicionales.** Actualmente utiliza datos locales del

workspace y métricas calculadas en el cliente.


Si en el futuro se conecta analítica externa:


- `GA_MEASUREMENT_ID` — configuración pública, no es un secreto.

- `POSTHOG_PUBLIC_KEY` — clave pública restringida por dominio, no reemplaza

una clave privada de servidor.

- `POSTHOG_API_KEY` — solo si se envían eventos desde el backend.


### Centro de funciones


Este menú no necesita credenciales propias. Reutiliza las de los módulos que

abre:


- Diagnóstico: sin secreto adicional.

- IA: `GEMINI_API_KEY`.

- Pagos: variables de Mercado Pago.

- SMS: variables del proveedor SMS elegido.

- Datos: credenciales del almacenamiento persistente que se conecte.

- Acceso: configuración pública de Firebase y, si se agregan sesiones

server-side, `SESSION_SECRET`.


### Pipeline Negocios, Empresas, Contactos y Tareas & Actividades


**No requieren secretos adicionales en la versión actual.** Los datos se

mantienen en el estado del CRM y en el almacenamiento local del navegador.


Para una persistencia server-side futura:


- `DATABASE_URL` — administrada automáticamente por Replit; no configurarla

manualmente.

- `DB_ENCRYPTION_KEY` — secreto recomendado si se cifran campos sensibles

antes de almacenarlos.


### Reportes & BI


**No requiere secretos adicionales actualmente.** Recharts y los datos del CRM

se ejecutan localmente.


Para una futura fuente de datos externa, guardar el token de esa fuente solo en

backend; no exponerlo con `VITE_`.


### Webmail Cloudflare (D1)


El inbox actual usa datos de demostración y no consume Cloudflare en runtime.

Para conectar el Worker y D1 en producción, usar:


| Variable | Tipo | Propósito |

| --- | --- | --- |

| `CLOUDFLARE_API_TOKEN` | Secreto | Token con permisos mínimos para el Worker/D1 necesarios. |

| `CLOUDFLARE_ACCOUNT_ID` | Configuración | Identificador de cuenta de Cloudflare. |

| `CLOUDFLARE_D1_DATABASE_ID` | Configuración | Identificador de la base D1. |

| `CLOUDFLARE_EMAIL_WORKER_URL` | Configuración | URL del Worker que expone el correo. |

| `CLOUDFLARE_EMAIL_WORKER_SECRET` | Secreto | Firma compartida entre ClientumCRM y el Worker. |

| `WEBMAIL_ENCRYPTION_KEY` | Secreto | Cifrado de contenido o credenciales si el Worker lo requiere. |


No enviar `CLOUDFLARE_API_TOKEN` al navegador.


### WhatsApp CRM


El módulo actual funciona como vista de CRM/demo. Para Meta WhatsApp Cloud

API, configurar únicamente en backend:


| Variable | Tipo | Propósito |

| --- | --- | --- |

| `WHATSAPP_ACCESS_TOKEN` | Secreto | Token de acceso de Meta. |

| `WHATSAPP_APP_SECRET` | Secreto | Validación de firmas de Meta. |

| `WHATSAPP_PHONE_NUMBER_ID` | Configuración | Número emisor de WhatsApp Business. |

| `WHATSAPP_BUSINESS_ACCOUNT_ID` | Configuración | Cuenta de WhatsApp Business. |

| `WHATSAPP_WEBHOOK_VERIFY_TOKEN` | Secreto | Token usado durante la verificación del webhook. |

| `WHATSAPP_WEBHOOK_URL` | Configuración | URL pública registrada en Meta. |


`WHATSAPP_ACCESS_TOKEN` y `WHATSAPP_APP_SECRET` nunca deben aparecer en

componentes React.


### Facturación AFIP (CAE)


La pantalla actual simula la emisión de comprobantes. Para producción,

solicitar el certificado y la clave privada por el mecanismo seguro de

secretos, nunca por chat:


| Variable | Tipo | Propósito |

| --- | --- | --- |

| `AFIP_CUIT` | Configuración sensible | CUIT emisor. |

| `AFIP_ENVIRONMENT` | Configuración | `homologacion` o `produccion`; no debe depender de un valor implícito. |

| `AFIP_CERTIFICATE_P12_BASE64` | Secreto | Certificado digital codificado para el backend. |

| `AFIP_PRIVATE_KEY` | Secreto | Clave privada del certificado, si el proveedor la entrega separada. |

| `AFIP_PRIVATE_KEY_PASSWORD` | Secreto | Contraseña del certificado. |

| `AFIP_WSAA_URL` | Configuración | Endpoint de autenticación WSAA. |

| `AFIP_WSFEX_URL` | Configuración | Endpoint del web service de facturación utilizado. |

| `AFIP_TOKEN_CACHE_KEY` | Secreto | Solo si se cachea el token de autorización cifrado. |


La generación del CAE debe estar protegida por permisos, idempotencia,

auditoría y separación estricta entre homologación y producción.


---


## 2. Ventas y cierre


### Propuestas & Presupuestos


**No requiere secretos en la versión actual.**


Si se habilita envío real de propuestas por correo:


- `RESEND_API_KEY` **o** `SENDGRID_API_KEY` — elegir un solo proveedor.

- `MAIL_FROM_ADDRESS` — configuración pública del remitente.

- `MAIL_FROM_NAME` — configuración pública del nombre remitente.

- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD` — alternativa SMTP;

`SMTP_PASSWORD` es secreto.


### Prospección Maps B2B


La app tiene fallback/demo y el backend de prospección no requiere Google Maps

para el resultado de demostración. Para geocodificación o Places real:


- `VITE_GOOGLE_MAPS_API_KEY` — clave pública restringida por dominio y APIs.

- `GOOGLE_MAPS_SERVER_API_KEY` — clave privada para llamadas server-side,

si se necesita separar el consumo backend.


No utilizar una clave con acceso irrestricto en el frontend.


### Lead Scoring MEDDIC


**No requiere secretos adicionales.** Si el scoring pasa a usar IA, reutiliza

`GEMINI_API_KEY`; no crear una clave separada por vista.


### Chatbot WhatsApp 24/7


Reutiliza:


- `GEMINI_API_KEY` — respuestas generativas, si se habilitan.

- `WHATSAPP_ACCESS_TOKEN`.

- `WHATSAPP_APP_SECRET`.

- `WHATSAPP_PHONE_NUMBER_ID`.

- `WHATSAPP_WEBHOOK_VERIFY_TOKEN`.


Ver la sección **WhatsApp CRM** para la descripción de cada variable.


### Campañas Masivas


Para WhatsApp, reutiliza las variables de Meta. Para email, elegir un solo

proveedor:


- `RESEND_API_KEY` — recomendado para una API de envío simple.

- `SENDGRID_API_KEY` — alternativa.

- `EMAIL_WEBHOOK_SIGNING_SECRET` — firma de eventos de entrega.

- `UNSUBSCRIBE_SIGNING_SECRET` — firma de enlaces de baja.


Los tokens de campaña deben tener límites de velocidad, consentimiento y

auditoría; no guardarlos en el navegador.


---


## 3. Inteligencia artificial


### Agent OS, Asistente Gemini, Estrategias GTM y Agente SDR Outreach


Todos estos módulos reutilizan una sola variable:


| Variable | Estado | Uso |

| --- | --- | --- |

| `GEMINI_API_KEY` | Requerida para IA real | Se consume en `server.ts` mediante los endpoints `/api/ai/*`. |


La aplicación tiene respuestas de fallback cuando la clave no está presente,

pero eso no equivale a una conexión real con Gemini. No exponer esta variable

con `VITE_GEMINI_API_KEY`.


---


## 4. Operaciones y sistema


### Cobros Mercado Pago


La UI actual genera links simulados. Para pagos reales:


| Variable | Tipo | Propósito |

| --- | --- | --- |

| `MERCADOPAGO_ACCESS_TOKEN` | Secreto | Crear preferencias, consultar pagos y emitir reembolsos autorizados. |

| `MERCADOPAGO_WEBHOOK_SECRET` | Secreto | Validar notificaciones de Mercado Pago. |

| `VITE_MERCADOPAGO_PUBLIC_KEY` | Pública restringida | Componentes de checkout del navegador, si se usan. |

| `MERCADOPAGO_ENVIRONMENT` | Configuración | `sandbox` o `production`. |

| `MERCADOPAGO_SELLER_ID` | Configuración sensible | Identificador de la cuenta receptora, si el flujo lo requiere. |


`MERCADOPAGO_ACCESS_TOKEN` y `MERCADOPAGO_WEBHOOK_SECRET` deben permanecer en

backend. Para este proyecto no están conectados todavía.


### Tienda Digital WhatsApp


Reutiliza las credenciales de WhatsApp y Mercado Pago. Si se conecta Shopify,

agregar solo mediante una integración administrada o backend:


- `SHOPIFY_SHOP_DOMAIN` — configuración.

- `SHOPIFY_STOREFRONT_ACCESS_TOKEN` — token de Storefront con alcance mínimo.

- `SHOPIFY_ADMIN_ACCESS_TOKEN` — secreto de administración; nunca frontend.

- `SHOPIFY_WEBHOOK_SECRET` — secreto para validar eventos.


No agregar credenciales de Shopify si la tienda continúa siendo local/demo.


### Campus Academia LMS


**No requiere secretos adicionales en la implementación actual.**


Si se habilitan videos o almacenamiento privado, usar el proveedor de storage

correspondiente en backend y una clave de firma temporal; no guardar URLs

firmadas permanentes en el código.


### Workflows & Flujos


El constructor actual trabaja con datos locales. Para ejecutar acciones

externas:


- `WEBHOOK_SIGNING_SECRET` — firma de webhooks propios.

- `WORKFLOW_ENCRYPTION_KEY` — cifrado de credenciales almacenadas en flujos.

- `N8N_API_KEY` — solo si se conecta n8n.

- `MAKE_WEBHOOK_SECRET` — solo si se conecta Make.

- `ZAPIER_WEBHOOK_SECRET` — solo si se conecta Zapier.


No se deben guardar tokens de terceros dentro de la configuración serializada

del workflow sin cifrado.


### Custom Objects Studio


**No requiere secretos adicionales.** Actualmente es un estudio local de

objetos, campos y registros.


### CSV Import & Export


**No requiere secretos adicionales.** Los archivos se procesan en el navegador.

Si en el futuro se suben a un bucket, usar URLs firmadas de vida corta y la

credencial únicamente en backend.


### Gestor de Dominios


Para Cloudflare DNS/SSL:


- `CLOUDFLARE_API_TOKEN` — secreto con permisos acotados a la zona.

- `CLOUDFLARE_ACCOUNT_ID` — configuración.

- `CLOUDFLARE_ZONE_ID` — configuración.

- `CLOUDFLARE_ZONE_NAME` — configuración.


No usar un token global de Cloudflare si alcanza un token limitado a una zona.


### Configuración General


#### Acceso autenticado y Firebase


Estas variables se usan para inicializar el SDK cliente de Firebase. No son

secretos de servidor, pero deben estar configuradas por entorno y protegidas

por restricciones de Firebase:


```text

VITE_FIREBASE_API_KEY=<configuración pública restringida>

VITE_FIREBASE_AUTH_DOMAIN=<dominio Firebase>

VITE_FIREBASE_PROJECT_ID=<id del proyecto>

VITE_FIREBASE_STORAGE_BUCKET=<bucket>

VITE_FIREBASE_MESSAGING_SENDER_ID=<sender id>

VITE_FIREBASE_APP_ID=<app id>

VITE_FIREBASE_MEASUREMENT_ID=<measurement id opcional>

```


Si se agregan operaciones Firebase Admin desde el servidor, usar una

integración o un secreto separado, por ejemplo:


- `FIREBASE_SERVICE_ACCOUNT_JSON` — secreto server-side.

- `FIREBASE_ADMIN_PROJECT_ID` — configuración.


No colocar una cuenta de servicio en `VITE_*`.


#### Sesiones del servidor


- `SESSION_SECRET` — secreto server-side para firmar sesiones, cookies o

CSRF si se incorpora un middleware de sesiones.


Actualmente ClientumCRM usa Firebase y flags de sesión del navegador; no hay

middleware Express que consuma `SESSION_SECRET`.


#### Integraciones y API Hub


El panel actual simula/guarda localmente parte de las integraciones. Para

producción:


- `GOOGLE_CLIENT_ID` — configuración OAuth.

- `GOOGLE_CLIENT_SECRET` — secreto OAuth server-side.

- `GOOGLE_OAUTH_REDIRECT_URI` — configuración.

- `SLACK_BOT_TOKEN` — secreto.

- `SLACK_SIGNING_SECRET` — secreto.

- `SLACK_WEBHOOK_URL` — secreto; no exponerlo en la UI.

- `API_KEY_PEPPER` — secreto para hashear claves internas antes de persistirlas.


Cuando exista un conector OAuth de Replit para el proveedor, preferirlo antes

que administrar manualmente `GOOGLE_CLIENT_SECRET` o tokens de refresco.


#### Auditoría y seguridad


**No requiere una credencial externa actualmente.** Para enviar alertas:


- `SECURITY_ALERT_WEBHOOK_SECRET` — firma de notificaciones.

- `SECURITY_ALERT_WEBHOOK_URL` — URL de destino; tratarla como sensible.


---


## 5. Menú lateral y asignación rápida


| Ítem del menú | Variables que realmente necesita |

| --- | --- |

| Resumen Ejecutivo | Ninguna |

| Centro de funciones | Reutiliza las del módulo elegido |

| Pipeline Negocios | Ninguna |

| Webmail Cloudflare (D1) | `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_D1_DATABASE_ID`, `CLOUDFLARE_EMAIL_WORKER_SECRET` |

| Empresas | Ninguna |

| Contactos | Ninguna |

| Tareas & Actividades | Ninguna |

| Reportes & BI | Ninguna; analítica futura según proveedor |

| WhatsApp CRM | `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_APP_SECRET`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_WEBHOOK_VERIFY_TOKEN` |

| Facturación AFIP (CAE) | `AFIP_CERTIFICATE_P12_BASE64`, `AFIP_PRIVATE_KEY`, `AFIP_PRIVATE_KEY_PASSWORD`, `AFIP_CUIT` |

| Propuestas & Presupuestos | Ninguna; email futuro: `RESEND_API_KEY` o `SENDGRID_API_KEY` |

| Prospección Maps B2B | `VITE_GOOGLE_MAPS_API_KEY` y, si aplica, `GOOGLE_MAPS_SERVER_API_KEY` |

| Lead Scoring MEDDIC | Ninguna; IA opcional: `GEMINI_API_KEY` |

| Chatbot WhatsApp 24/7 | Variables de WhatsApp + `GEMINI_API_KEY` opcional |

| Campañas Masivas | WhatsApp o `RESEND_API_KEY`/`SENDGRID_API_KEY` |

| Agent OS | `GEMINI_API_KEY` |

| Asistente Gemini | `GEMINI_API_KEY` |

| Estrategias GTM | `GEMINI_API_KEY` |

| Agente SDR Outreach | `GEMINI_API_KEY` + canal de envío elegido |

| Cobros Mercado Pago | `MERCADOPAGO_ACCESS_TOKEN`, `MERCADOPAGO_WEBHOOK_SECRET`, `VITE_MERCADOPAGO_PUBLIC_KEY` |

| Tienda Digital WhatsApp | WhatsApp + Mercado Pago; Shopify solo si se conecta |

| Campus Academia LMS | Ninguna en la versión actual |

| Workflows & Flujos | `WORKFLOW_ENCRYPTION_KEY` + secretos del proveedor conectado |

| Custom Objects Studio | Ninguna |

| CSV Import & Export | Ninguna |

| Gestor de Dominios | `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_ZONE_ID` |

| Configuración General | Firebase `VITE_*`, `SESSION_SECRET` si se agregan sesiones |


---


## 6. SMS: proveedor a elegir


El Centro de funciones ya prepara la configuración y el mensaje SMS, pero no

hay proveedor conectado en el código actual. Elegir **un solo** proveedor:


### Opción recomendada: Twilio


- `TWILIO_ACCOUNT_SID` — configuración sensible.

- `TWILIO_AUTH_TOKEN` — secreto.

- `TWILIO_MESSAGING_SERVICE_SID` — configuración.

- `TWILIO_FROM_NUMBER` — configuración.

- `TWILIO_WEBHOOK_SIGNING_SECRET` — secreto para callbacks.


### Alternativas


- MessageBird: `MESSAGEBIRD_ACCESS_KEY`, `MESSAGEBIRD_ORIGINATOR`,

`MESSAGEBIRD_WEBHOOK_SECRET`.

- Vonage: `VONAGE_API_KEY`, `VONAGE_API_SECRET`, `VONAGE_BRAND_NAME`,

`VONAGE_SIGNATURE_SECRET`.


No solicitar ni guardar las variables de las tres opciones a la vez. La

conexión debe gestionarse mediante una integración o Secrets y el envío real

debe vivir en backend.


---


## 7. Checklist antes de producción


- [ ] Configurar `GEMINI_API_KEY` en Replit Secrets para habilitar IA real.

- [ ] Reemplazar los fallbacks hardcodeados de Firebase por `VITE_FIREBASE_*`.

- [ ] Verificar que las reglas de Firebase Auth/Firestore no permitan acceso

público accidental.

- [ ] Elegir un proveedor de pagos y validar webhooks con firma.

- [ ] Elegir un único proveedor SMS y configurar sus credenciales en backend.

- [ ] Conectar WhatsApp mediante Meta antes de habilitar envíos.

- [ ] Separar homologación/producción para AFIP.

- [ ] Rotar cualquier token que haya sido expuesto en código o logs.

- [ ] No solicitar manualmente `DATABASE_URL` ni otras variables administradas

por Replit.

- [ ] Revisar permisos por rol antes de habilitar cobros, exportaciones,

facturación o administración de credenciales.

Este documento técnico sirve como guía de auditoría, configuración de variables de entorno y arquitectura de seguridad para el despliegue de ClientumCRM.

A continuación se detallan la distribución de secretos, la matriz de integración por módulo y las recomendaciones críticas para la puesta en producción.
1. Arquitectura de variables y secretos

Para garantizar el cumplimiento del principio de menor privilegio y evitar la fuga de credenciales, la configuración se organiza en tres niveles estricta y funcionalmente separados:

                  ┌─────────────────────────────────────────┐
                  │          ENTORNO CLIENTUM CRM           │
                  └────────────────────┬────────────────────┘
                                       │
         ┌─────────────────────────────┼─────────────────────────────┐
         ▼                             ▼                             ▼
┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│ Replit Secrets / │         │   Firebase /     │         │ API Keys por     │
│ Server Env       │         │   Client Env     │         │ Usuario          │
├──────────────────┤         ├──────────────────┤         ├──────────────────┤
│ • Backend only   │         │ • Public / Build │         │ • DB Persisted   │
│ • Privado        │         │ • Navegador      │         │ • Scoped         │
│ • process.env    │         │ • VITE_*         │         │ • Revocables     │
└──────────────────┘         └──────────────────┘         └──────────────────┘

    Secretos de la plataforma (Backend / Replit Secrets):

        Variables de entorno leídas únicamente mediante process.env en Node.js/Express.

        Nunca deben empaquetarse en el cliente ni exponerse con el prefijo VITE_.

        Ejemplos: GEMINI_API_KEY, SESSION_SECRET, MERCADOPAGO_ACCESS_TOKEN, AFIP_PRIVATE_KEY.

    Configuración pública del cliente (Variables VITE_*):

        Inyectadas durante el proceso de empaquetado (Vite) y visibles en el código del navegador.

        Deben estar estrictamente restringidas por dominio o IP en los paneles de control correspondientes (Google Cloud Console, Firebase Console).

        Ejemplos: VITE_FIREBASE_API_KEY, VITE_GOOGLE_MAPS_API_KEY, VITE_MERCADOPAGO_PUBLIC_KEY.

    API Keys orientadas al usuario / Tenant (API Hub):

        Claves generadas dinámicamente desde Configuración → Integraciones & API Hub.

        Pertenecen a un user_id específico y contienen restricciones de alcance (scopes).

        Solo se muestra el token completo una vez al crearse; en la base de datos se almacena únicamente el hash (API_KEY_PEPPER).

2. Matriz de integración y dependencias por módulo

El siguiente esquema resume qué variables requiere cada módulo para pasar del modo Demo/Fallback al entorno Producción:
Módulo CRM	Requiere secreto	Principales variables requeridas	Nivel de ejecución
Resumen Ejecutivo / Contactos / Empresas / Pipeline	No	Ninguna (Estado local / DB)	Cliente / Server DB
Agentes IA, Copilot, SDR, GTM	Sí	GEMINI_API_KEY	Backend (/api/ai/*)
WhatsApp CRM & Chatbot 24/7	Sí	WHATSAPP_ACCESS_TOKEN, WHATSAPP_APP_SECRET, WHATSAPP_PHONE_NUMBER_ID	Backend / Webhook
Facturación AFIP (CAE)	Sí	AFIP_CUIT, AFIP_CERTIFICATE_P12_BASE64, AFIP_PRIVATE_KEY	Backend
Cobros Mercado Pago	Sí	MERCADOPAGO_ACCESS_TOKEN, MERCADOPAGO_WEBHOOK_SECRET	Backend / Webhook
Webmail Cloudflare (D1)	Sí	CLOUDFLARE_API_TOKEN, CLOUDFLARE_D1_DATABASE_ID	Backend / Worker
Prospección Maps B2B	Sí	VITE_GOOGLE_MAPS_API_KEY (Client), GOOGLE_MAPS_SERVER_API_KEY (Server)	Híbrido
Campañas Masivas / Emailing	Sí	RESEND_API_KEY o SENDGRID_API_KEY	Backend
Notificaciones SMS	Sí	TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN (u otro proveedor elegido)	Backend
Gestor de Dominios	Sí	CLOUDFLARE_API_TOKEN, CLOUDFLARE_ZONE_ID	Backend
3. Plan de acción inmediato para despliegue

Para habilitar la infraestructura de producción sin vulnerar la seguridad de la plataforma, aplique la siguiente secuencia de configuración:
Fase 1: Habilitación de Inteligencia Artificial

    Obtener una clave de API válida en Google AI Studio.

    Ir a Replit Secrets (panel lateral de Replit).

    Agregar la clave con el nombre exacto: GEMINI_API_KEY.

    Reiniciar el servidor para que los endpoints de /api/ai/* dejen de emitir respuestas en modo simulación.

Fase 2: Configuración del cliente y autenticación (Firebase)

    Copiar las credenciales del proyecto desde la Consola de Firebase.

    Definir en el entorno de build/Replit las variables con prefijo VITE_:
    Fragmento de código

    VITE_FIREBASE_API_KEY=AIzaSy...
    VITE_FIREBASE_AUTH_DOMAIN=clientum-crm.firebaseapp.com
    VITE_FIREBASE_PROJECT_ID=clientum-crm
    VITE_FIREBASE_STORAGE_BUCKET=clientum-crm.appspot.com
    VITE_FIREBASE_MESSAGING_SENDER_ID=1234567890
    VITE_FIREBASE_APP_ID=1:1234567890:web:abc123def456

    Eliminar los valores de fallback hardcodeados en el código fuente del cliente.

Fase 3: Pasarelas de pago y mensajería

    Mercado Pago: Configurar MERCADOPAGO_ACCESS_TOKEN en backend y registrar el endpoint de webhook (/api/webhooks/mercadopago) usando la firma de validación MERCADOPAGO_WEBHOOK_SECRET.

    Meta / WhatsApp: Inscribir la App en el portal Meta for Developers y agregar WHATSAPP_ACCESS_TOKEN y WHATSAPP_APP_SECRET exclusivamente en el backend.

4. Protocolo de auditoría y rotación de credenciales

    Aviso de seguridad: Si alguna clave o secreto ha sido pegado previamente en el chat, commiteado en el repositorio de código o expuesto en un archivo markdown público/compartido, considere esa credencial comprometida.

    Revocación: Acceda al panel del proveedor (Google Cloud, Meta, Mercado Pago, Cloudflare) y revoque la clave expuesta inmediatamente.

    Generación: Cree un nuevo par de credenciales con el mínimo alcance (scope) requerido.

    Inyección: Cargue el nuevo valor directamente en el gestor de secretos de la infraestructura (Replit Secrets) sin imprimir el valor en consola ni guardarlo en buffers locales.

ATERNATIVAS FREE A TODO

Para desplegar, operar e integrar ClientumCRM sin incurrir en costos de suscripción ni licencias de software, se detalla un mapa exhaustivo de alternativas 100% gratuitas (Free Tier permanente o Self-Hosted) para cada capa de la arquitectura, servicios externos, IA y módulos del sistema.  
MD
1. Inteligencia Artificial y Modelos Generativos

Actualmente, el sistema requiere la clave GEMINI_API_KEY para los módulos Copilot, CMO, Agentes IA, SDR Outreach y Estrategias GTM.  
MD
Función / Uso	Proveedor / Servicio	Capa Gratuita / Free Tier	Alternativa Self-Hosted (Local)

API de IA Principal (Remplaza GEMINI_API_KEY)  
MD
	Google AI Studio	Gratis (hasta 15 RPM / 1M TPM en Gemini 1.5 Pro/Flash). Generación de claves de API sin tarjeta de crédito.	Ollama o LM Studio ejecutando modelos open-source como Llama-3.1-8B, Mistral-7B o Qwen-2.5.
Enrutador / Aggregator de IA	OpenRouter.ai	Incluye acceso a modelos gratuitos (e.g., meta-llama/llama-3-8b-instruct:free, mistralai/mistral-7b-instruct:free).	LiteLLM (Proxy local para estandarizar APIs compatibles con OpenAI).
Agentes Autónomos & Workflows de IA	n8n Community Edition	Planes Cloud tienen costo, pero la versión self-hosted es totalmente gratuita y sin límites de ejecución.	Flowise AI o Langflow (Constructores visuales de agentes IA locales).
2. Base de Datos, Persistencia y Backend

El proyecto utiliza Firebase en el frontend y referencia la administración automática de base de datos en Replit.  
MD
Módulo / Requisito	Opción Cloud Free Tier	Opción Self-Hosted / Open Source
Base de Datos Relacional / Documental	Supabase Free Tier (500 MB PostgreSQL, Auth, Realtime) o Neon.tech (0.5 GiB PostgreSQL).	PostgreSQL o MongoDB Community Edition desplegado en un VPS propio o Docker local.
Autenticación de Usuarios	

Firebase Auth Free Tier (hasta 50,000 MAU para Email/Password y Google Sign-In).  
MD
	Supabase Auth, Clerk (plan gratuito hasta 10k MAU) o Keycloak.

Base de Datos Serverless edge (D1 / Webmail)

  
MD
	

Cloudflare D1 Free Tier (hasta 5,000,000 lecturas y 100,000 escrituras diarias gratis).  
MD
	SQLite local montado en el contenedor del servidor Node.js/Express.
Almacenamiento de Archivos / adjuntos	Cloudflare R2 (10 GB al mes gratis, sin costos de egreso de ancho de banda).	MinIO (Servidor de almacenamiento de objetos compatible con AWS S3).
3. Mensajería, Comunicaciones y Redes Sociales

Para sustituir costos en servicios transaccionales de WhatsApp, Emailing y SMS:  
MD
Módulo CRM	Proveedor Oficial (de Pago / Límite estricto)	Alternativa 100% Gratuita (Free Tier / Open Source)

WhatsApp CRM & Chatbot 24/7

  
MD
	

Meta WhatsApp Cloud API (Las primeras 1,000 conversaciones iniciadas por el usuario al mes son gratuitas).  
MD
	

Evolution API / Baileys / WPPConnect:

Servidores Node.js de código abierto que vinculan un número de WhatsApp mediante código QR (sin pagar por mensaje a Meta).

Transaccional Emailing & Campañas

  
MD
	

SendGrid / Resend (Planes gratuitos con límites de 100 envíos/día).  
MD
	

Brevo (anteriormente Sendinblue):

300 emails/día gratuitos.

SMTP propio configurado en servidor web con TLS mediante Postfix/Exim.

Envío de SMS

  
MD
	

Twilio / MessageBird / Vonage (Requieren saldo prepago por mensaje).  
MD
	

Android SMS Gateway (Proyectos open-source como SMS Gateway API o Kannel):

Transforman un teléfono móvil Android en una pasarela SMS utilizando el plan de SMS ilimitados de la tarjeta SIM local.

Webmail & Email Routing

  
MD
	Google Workspace / Microsoft 365.	

Cloudflare Email Routing + Workers:

Enrutamiento gratuito de correos entrantes de un dominio propio hacia casillas personales o bases D1.  
MD
4. Facturación e Integraciones Financieras

Para cubrir las capacidades de cobro y emisión de comprobantes fiscales:  
MD
Módulo CRM	Integración Comercial	Alternativa Gratuita / Directa

Facturación AFIP (CAE)

  
MD
	Gestores de facturación de terceros con abono mensual (e.g., Facturante, TusFacturas).	

Conexión Directa WSAA / WSFEv1 (AFIP):

Uso directo de los Web Services SOAP oficiales de AFIP mediante la librería open-source afip.js en Node.js. Los certificados .crt y .key se gestionan sin costo desde el portal con Clave Fiscal.  
MD+ 1

Cobros y Pasarela de Pagos

  
MD
	Integraciones avanzadas con suscripción.	

Mercado Pago API Directa:

Sin costo de mantenimiento fijo ni suscripción. Solo se deduce la comisión por venta cobrada (en el entorno Sandbox de pruebas no hay cargos directos).  
MD
5. Plataformas de Hosting, Despliegue y Dominios

Para reemplazar las limitaciones de proyectos en Replit o alojamiento de pago:  
MD
Infraestructura	Proveedor Comercial	Alternativa Gratuita Recomendada
Hosting Frontend (React + Vite)	Vercel Pro / Netlify Paid.	

Cloudflare Pages o Vercel Hobby Plan:

Despliegues ilimitados con HTTPS automático y vinculación de dominios personalizados gratis.
Hosting Backend (Node.js / Express)	AWS EC2 / DigitalOcean / Heroku.	Render.com (Servicios Web gratuitos con suspensión por inactividad) o Koyeb / Fly.io (Planes de entrada sin costo).
Infraestructura Integrada (PaaS Self-Hosted)	Heroku / AWS.	

Coolify / Dokku / CapRover:

Alternativas open-source a Heroku para instalar en un servidor local o máquina de pruebas y desplegar contenedores Docker con un clic.

Gestión de DNS y SSL

  
MD
	Registradores de dominio con cobro por certificado SSL.	

Cloudflare Free Account:

Protección DDoS, CDN, DNS ultrarrápido y certificados SSL/TLS automáticos y gratuitos.  
MD
6. Arquitectura General Recomendada "Zero-Cost"

Para operativizar ClientumCRM sin gasto de infraestructura:  
MD

                           ┌────────────────────────────────────────┐
                           │      Navegador Web del Usuario         │
                           └──────────────────┬─────────────────────┘
                                              │
                      ┌───────────────────────┴───────────────────────┐
                      ▼                                               ▼
   ┌────────────────────────────────────┐          ┌────────────────────────────────────┐
   │     Frontend (React + Vite)        │          │    Backend (Node.js / Express)      │
   │  Alojado en: Cloudflare Pages /    │          │    Alojado en: Render / Koyeb       │
   │  Vercel Free Tier                  │          │    Free Tier                       │
   └──────────────────┬─────────────────┘          └──────────────────┬─────────────────┘
                      │                                               │
        ┌─────────────┴─────────────┐                   ┌─────────────┴─────────────┐
        ▼                           ▼                   ▼                           ▼
┌───────────────┐           ┌───────────────┐   ┌───────────────┐           ┌───────────────┐
│ Auth & Data   │           │ Maps Client   │   │ IA Generativa │           │ WhatsApp /    │
│ Supabase /    │           │ OpenStreetMap │   │ Google AI     │           │ Mensajería    │
│ Firebase Free │           │ + Leaflet     │   │ Studio API    │           │ Evolution API │
└───────────────┘           └───────────────┘   └───────────────┘           └───────────────┘

    Frontend: Desplegado en Cloudflare Pages o Vercel enlazado a un repositorio público/privado de GitHub.  
    MD

    Backend: Ejecutado en Render o en un servidor local mediante Coolify.

    Motor de IA: Consumo de la API gratuita de Google AI Studio (GEMINI_API_KEY).  
    MD

    Base de Datos & Auth: Supabase Free Tier o Firebase para almacenamiento y sesiones de usuario.  
    MD

    WhatsApp & Mensajería: Instancia local de Evolution API vinculada por código QR.

# ============================================================

# CLIENTUMCRM - PLATFORM ENVIRONMENT

# ============================================================

# Este archivo es una plantilla. Los valores reales van en Replit Secrets

# y nunca se deben commitear ni pegar en el chat.


# --- Core server configuration ---

# Secret consumed by the AI endpoints in server.ts.

GEMINI_API_KEY=

# Reserved for server-side sessions/cookies/CSRF when enabled.

SESSION_SECRET=

# Public runtime configuration (not a secret).

APP_URL=

NODE_ENV=development


# --- Firebase client configuration ---

# These VITE_* values are bundled into the browser. They are public

# configuration, not server secrets, and must be restricted in Firebase.

VITE_FIREBASE_API_KEY=

VITE_FIREBASE_AUTH_DOMAIN=

VITE_FIREBASE_PROJECT_ID=

VITE_FIREBASE_STORAGE_BUCKET=

VITE_FIREBASE_MESSAGING_SENDER_ID=

VITE_FIREBASE_APP_ID=

VITE_FIREBASE_MEASUREMENT_ID=


# ============================================================

# OPTIONAL PLATFORM CONNECTORS

# Add only when the corresponding module is connected in backend code.

# Keep server-only credentials outside VITE_* variables.

# ============================================================


# Payments

MERCADOPAGO_ACCESS_TOKEN=

MERCADOPAGO_WEBHOOK_SECRET=

VITE_MERCADOPAGO_PUBLIC_KEY=

MERCADOPAGO_ENVIRONMENT=sandbox


# SMS: choose one provider, do not configure all three.

TWILIO_ACCOUNT_SID=

TWILIO_AUTH_TOKEN=

TWILIO_MESSAGING_SERVICE_SID=

TWILIO_FROM_NUMBER=

TWILIO_WEBHOOK_SIGNING_SECRET=


# WhatsApp Cloud API

WHATSAPP_ACCESS_TOKEN=

WHATSAPP_APP_SECRET=

WHATSAPP_PHONE_NUMBER_ID=

WHATSAPP_BUSINESS_ACCOUNT_ID=

WHATSAPP_WEBHOOK_VERIFY_TOKEN=


# Cloudflare Webmail / D1 / Domains

CLOUDFLARE_API_TOKEN=

CLOUDFLARE_ACCOUNT_ID=

CLOUDFLARE_D1_DATABASE_ID=

CLOUDFLARE_ZONE_ID=

CLOUDFLARE_EMAIL_WORKER_URL=

CLOUDFLARE_EMAIL_WORKER_SECRET=


# AFIP

AFIP_CUIT=

AFIP_ENVIRONMENT=homologacion

AFIP_CERTIFICATE_P12_BASE64=

AFIP_PRIVATE_KEY=

AFIP_PRIVATE_KEY_PASSWORD=


# Google / email / Slack

GOOGLE_CLIENT_ID=

GOOGLE_CLIENT_SECRET=

GOOGLE_OAUTH_REDIRECT_URI=

RESEND_API_KEY=

SENDGRID_API_KEY=

SLACK_BOT_TOKEN=

SLACK_SIGNING_SECRET=

SLACK_WEBHOOK_URL=


# Internal platform safeguards

WORKFLOW_ENCRYPTION_KEY=

WEBHOOK_SIGNING_SECRET=

API_KEY_PEPPER=

A continuación se presenta una guía práctica y estructurada para reemplazar cada componente pago o dependiente de la nube por alternativas 100% gratuitas y sin límites de pago, utilizando servicios con Free Tier perpetuo o componentes Open Source que pueden ejecutarse de forma local o autocanjeada (Self-Hosted).
1. Núcleo, Inteligencia Artificial y Agentes

Para eliminar la dependencia de un plan pago de modelos de lenguaje o evitar costos por token:

    Inferencia de IA sin costo (API Cloud):

        Google AI Studio (Gemini 1.5 Flash / Pro): Otorga un plan gratuito de hasta 15 solicitudes por minuto (RPM) y 1.000.000 de tokens por minuto (TPM). Permite generar la GEMINI_API_KEY sin ingresar tarjeta de crédito.

        OpenRouter (Modelos Free): Funciona como enrutador y ofrece acceso a modelos como meta-llama/llama-3.1-8b-instruct:free o mistralai/mistral-7b-instruct:free.

    Motor de IA Local / Self-Hosted (100% Privado):

        Ollama / LM Studio: Permite ejecutar modelos Open Source (Llama 3.1, Mistral, Qwen 2.5) localmente en un servidor o máquina de desarrollo. Expone una API compatible con OpenAI para conectar directamente al backend Node.js (/api/ai/*).

    Automatización y Agentes:

        n8n Community Edition / Flowise AI: La versión self-hosted en Docker es gratuita e ilimitada para crear orquestaciones complejas de agentes y conectar webhooks.

2. Base de Datos, Autenticación y Almacenamiento

Sustitutos sin cargo para PostgreSQL de pago o almacenamiento en la nube:

    Base de Datos Relacional (PostgreSQL):

        Supabase (Tier Gratuito): Incluye 500 MB de base de datos PostgreSQL, panel visual, cliente API y extensión Vectorial (pgvector) para búsquedas semánticas.

        Neon.tech: Proporciona un clúster serverless de PostgreSQL gratuito (hasta 0.5 GiB de almacenamiento y cómputo Always-On).

    Autenticación de Usuarios:

        Firebase Auth / Supabase Auth: Manejan hasta 50.000 usuarios activos mensuales (MAU) sin costo para inicio de sesión por correo/contraseña y proveedores OAuth (Google, GitHub).

    Almacenamiento de Archivos (Object Storage):

        Cloudflare R2: Proporciona 10 GB de almacenamiento mensual y lecturas/escrituras masivas sin cobro de ancho de banda (egreso de datos gratis).

        MinIO (Self-Hosted): Servidor de almacenamiento de objetos compatible con la API de Amazon S3 para instalar en un servidor propio.

3. Mensajería, Correo y Notificaciones

Opciones para evitar pasarelas SMS o APIs de pago:

    WhatsApp CRM & Chatbots:

        Meta WhatsApp Cloud API (Oficial): La capa gratuita de Meta incluye 1.000 conversaciones iniciadas por usuarios al mes de forma gratuita.

        Evolution API / Baileys (Gratis e Ilimitado - Self-Hosted): Motores de código abierto en Node.js que convierten cualquier número de WhatsApp (personal o Business) en una API mediante código QR, omitiendo las comisiones por mensaje.

    Emails Transaccionales y Campañas:

        Brevo (Sendinblue): Permite enviar hasta 300 correos electrónicos diarios en su plan gratuito permanente.

        Cloudflare Email Routing + Workers: Permite redirigir correos entrantes de un dominio propio hacia casillas personales o almacenarlos en una base de datos Cloudflare D1 sin costo.

    Notificaciones SMS sin costo:

        Android SMS Gateway: Aplicaciones de código abierto que transforman un teléfono móvil Android en un servidor backend de SMS, aprovechando los paquetes de mensajes ilimitados de la SIM telefónica.

4. Facturación, Cobros y Servicios Locales

Servicios para operar comercialmente en Argentina sin tarifas mensuales fijas:

    Facturación Electrónica AFIP (CAE):

        Conexión Directa WSAA / WSFEv1: Utilizar la librería Open Source afip.js para consumir los Web Services SOAP oficiales de la AFIP. Los certificados digitales (.crt y .key) se tramitan de forma gratuita mediante Clave Fiscal en el portal de la AFIP.

    Pasarela de Cobros (Mercado Pago):

        Mercado Pago API Directa: Sin costo de mantenimiento mensual ni suscripción. Solo descuenta la comisión por cobro realizado. El entorno Sandbox de pruebas es 100% gratuito.

5. Infraestructura de Despliegue (Hosting & Servidores)

Mapa de despliegue con costo $0:
Componente	Opción Cloud Gratis	Opción Self-Hosted / Local
Frontend (React + Vite)	

Cloudflare Pages / Vercel Hobby

(Despliegues e historial ilimitados, CDN global y SSL automático).
	Nginx o Caddy en un servidor personal.
Backend (Node.js / Express)	

Render.com / Koyeb

(Servicios Web gratuitos con soporte HTTPS).
	

Coolify / Dokku

(Alternativas de código abierto a Heroku para administrar contenedores Docker).
Gestión DNS y SSL	

Cloudflare Free

(Protección DDoS, DNS ultrarrápido y certificados SSL gratuitos).
	

Certbot (Let's Encrypt)

(Certificados SSL auto-renovables).
6. Configuración de Variables de Entorno (.env)

A continuación se muestra el archivo de entorno adaptado para operar exclusivamente con proveedores en sus planes gratuitos o locales:
Fragmento de código

# ============================================================
# CLIENTUMCRM - CONFIGURACIÓN 100% GRATUITA / FREE-TIER
# ============================================================

# --- Servidor y Backend ---
NODE_ENV=development
APP_URL=http://localhost:5000
SESSION_SECRET=clave_aleatoria_para_sesiones_locales_123

# --- Motor de Inteligencia Artificial (Elegir Google AI Studio u Ollama) ---
# Opción A: Google AI Studio (Gratis hasta 15 RPM / 1M TPM)
GEMINI_API_KEY=tu_clave_de_google_ai_studio

# Opción B: Ollama / LM Studio (Local e Ilimitado)
# AI_BASE_URL=http://localhost:11434/v1
# AI_MODEL=llama3.1

# --- Autenticación y Base de Datos (Firebase / Supabase) ---
VITE_FIREBASE_API_KEY=tu_api_key_publica
VITE_FIREBASE_AUTH_DOMAIN=tu-proyecto.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=tu-proyecto
VITE_FIREBASE_STORAGE_BUCKET=tu-proyecto.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef
DATABASE_URL=postgresql://postgres:password@db.supabase.co:5432/postgres

# --- WhatsApp CRM (Evolution API Self-Hosted o Meta Cloud API Free) ---
# Opción A: Evolution API (WhatsApp conectado por QR)
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=clave_api_local

# Opción B: Meta Cloud API (1.000 conversaciones gratis/mes)
WHATSAPP_ACCESS_TOKEN=token_temporal_o_permanente_meta
WHATSAPP_PHONE_NUMBER_ID=id_numero_whatsapp
WHATSAPP_WEBHOOK_VERIFY_TOKEN=token_de_verificacion_webhook

# --- Envíos de Email (Brevo: 300 envíos/día gratis) ---
RESEND_API_KEY=
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=tu_email_registrado@domain.com
SMTP_PASSWORD=tu_clave_smtp_brevo
MAIL_FROM_ADDRESS=tu_email_registrado@domain.com

# --- Facturación AFIP (Librería afip.js directa sin intermediarios) ---
AFIP_CUIT=20123456789
AFIP_ENVIRONMENT=homologacion
AFIP_CERTIFICATE_P12_BASE64=contenido_certificado_base64
AFIP_PRIVATE_KEY=contenido_clave_privada_pem

# --- Cobros Mercado Pago (Sin costo fijo de mantenimiento) ---
MERCADOPAGO_ACCESS_TOKEN=TEST-1234567890-access-token
MERCADOPAGO_WEBHOOK_SECRET=secreto_para_validar_webhooks
VITE_MERCADOPAGO_PUBLIC_KEY=TEST-1234567890-public-key
MERCADOPAGO_ENVIRONMENT=sandbox

# --- Servidores y Seguridad Interna ---
WORKFLOW_ENCRYPTION_KEY=clave_32_caracteres_para_cifrar
WEBHOOK_SIGNING_SECRET=secreto_firma_webhooks
API_KEY_PEPPER=secreto_para_hashear_api_keys

Opción Cloud Gratis

Elegir la Opción Cloud Gratis permite desplegar la arquitectura completa de ClientumCRM sin costo de servidores ni suscripciones mensuales, aprovechando los límites Free Tier perpetuos de los principales proveedores en la nube.
1. Mapa de Infraestructura Cloud Gratuita ($0/mes)

                       ┌────────────────────────────────────────┐
                       │     Navegador del Usuario Final        │
                       └──────────────────┬─────────────────────┘
                                          │
                  ┌───────────────────────┴───────────────────────┐
                  ▼                                               ▼
┌───────────────────────────────────┐           ┌───────────────────────────────────┐
│     Frontend (React + Vite)       │           │     Backend (Node.js / Express)   │
│  Cloudflare Pages / Vercel Free   │           │   Render / Koyeb Free Tier        │
│  (Ancho de banda e historial 100%)│           │   (Servicios Web con SSL gratis)  │
└─────────────────┬─────────────────┘           └─────────────────┬─────────────────┘
                  │                                               │
      ┌───────────┴───────────┐                       ┌───────────┴───────────┐
      ▼                       ▼                       ▼                       ▼
┌───────────────┐     ┌───────────────┐       ┌───────────────┐       ┌───────────────┐
│ Auth & DB     │     │ Storage       │       │ IA Engine     │       │ Email & SMS   │
│ Supabase      │     │ Cloudflare R2 │       │ Google AI     │       │ Brevo /       │
│ PostgreSQL    │     │ (10 GB gratis)│       │ Studio (1.5)  │       │ Android SIM   │
└───────────────┘     └───────────────┘       └───────────────┘       └───────────────┘

2. Servicios Cloud Recomendados
A. Hosting Frontend

    Cloudflare Pages: Permite conectar el repositorio de GitHub para desplegar el frontend de React. Incluye ancho de banda ilimitado, SSL automático y CDN global sin costo de mantenimiento.

    Vercel Hobby: Alternativa gratuita para publicar aplicaciones web con integración continua (CI/CD) inmediata.

B. Hosting Backend (Servidor Node.js / Express)

    Render.com: Servicio web gratuito que ejecuta el backend de Node.js. Inicia automáticamente con las peticiones y se hiberna tras 15 minutos de inactividad para ahorrar recursos.

    Koyeb: Asigna una instancia gratuita con 512 MB de RAM y 100 GB de transferencia mensual, ideal para mantener microservicios ligeros activos.

C. Base de Datos y Autenticación

    Supabase Free Tier: Proporciona una base de datos PostgreSQL administrada (500 MB), extensión vectorial (pgvector) para búsquedas con IA, sistema de autenticación de usuarios (hasta 50.000 usuarios activos mensuales) y APIs REST/GraphQL integradas.

D. Almacenamiento de Archivos y Adjuntos

    Cloudflare R2: Otorga 10 GB de almacenamiento de objetos al mes con $0 de costo por egreso de datos (transferencia de descarga sin cobro).

E. Motores de Inteligencia Artificial

    Google AI Studio (GEMINI_API_KEY): Ofrece acceso a los modelos Gemini 1.5 Flash y Gemini 1.5 Pro de forma gratuita sin necesidad de ingresar tarjeta de crédito, soportando hasta 15 solicitudes por minuto (RPM) y 1.000.000 de tokens por minuto (TPM).

F. Mensajería y Facturación

    Brevo (Sendinblue): Plan gratuito de envío transaccional de correo con un límite de 300 emails por día.

    Meta WhatsApp Cloud API: Incluye las primeras 1.000 conversaciones iniciadas por usuarios al mes sin tarifa comercial.  
    MD

    AFIP (Web Services Oficiales): Generación de certificados y facturas electrónicas directamente mediante la librería afip.js y los endpoints oficiales de homologación y producción.  
    MD

3. Ventajas de la Opción Cloud Gratis

    Cero costo operativo: Todos los servicios seleccionados cuentan con planes Free Tier de renovación automática mensual o diaria.

    Mantenimiento nulo: Las bases de datos, certificados de seguridad SSL y servidores son administrados automáticamente por los proveedores.

    Preparado para escalar: Si la demanda del CRM crece, cada servicio permite realizar un upgrade a planes de pago con un solo clic sin necesidad de reescribir la aplicación.

AHORA Configuración de Variables de Entorno (.env) FINAL

Aquí tienes la plantilla FINAL del archivo .env optimizada exclusivamente para la Opción Cloud Gratis ($0/mes), lista para copiar y pegar en los gestores de secretos de Render/Koyeb (backend) y Cloudflare Pages/Vercel (frontend).
Fragmento de código

# ============================================================
# CLIENTUMCRM - PLANTILLA FINAL 100% CLOUD GRATIS ($0/MES)
# ============================================================

# ------------------------------------------------------------
# 1. CORE SERVER CONFIGURATION (Backend - Render / Koyeb)
# ------------------------------------------------------------
NODE_ENV=production
APP_URL=https://tu-backend.onrender.com
SESSION_SECRET=genera_un_hash_aleatorio_32_caracteres_aqui

# ------------------------------------------------------------
# 2. MOTOR DE IA (Google AI Studio - Free Tier 15 RPM / 1M TPM)
# ------------------------------------------------------------
# Obtenida sin tarjeta de crédito desde: https://aistudio.google.com/
GEMINI_API_KEY=tu_clave_api_de_google_ai_studio

# ------------------------------------------------------------
# 3. BASE DE DATOS Y AUTENTICACIÓN (Supabase Free Tier)
# ------------------------------------------------------------
# Base de datos PostgreSQL (500 MB gratis): Dashboard -> Project Settings -> Database
DATABASE_URL=postgresql://postgres.xxxx:tu_password@aws-0-sa-east-1.pooler.supabase.com:6543/postgres

# Configuración Pública Firebase / Supabase Client (Vite Frontend)
VITE_FIREBASE_API_KEY=tu_firebase_api_key
VITE_FIREBASE_AUTH_DOMAIN=tu-proyecto.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=tu-proyecto
VITE_FIREBASE_STORAGE_BUCKET=tu-proyecto.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=1234567890
VITE_FIREBASE_APP_ID=1:1234567890:web:abcdef123456

# ------------------------------------------------------------
# 4. ALMACENAMIENTO DE ARCHIVOS (Cloudflare R2 - 10 GB Gratis)
# ------------------------------------------------------------
CLOUDFLARE_ACCOUNT_ID=tu_account_id_cloudflare
R2_ACCESS_KEY_ID=tu_r2_access_key
R2_SECRET_ACCESS_KEY=tu_r2_secret_key
R2_BUCKET_NAME=clientum-crm-storage

# ------------------------------------------------------------
# 5. EMAILING Y CAMPAÑAS (Brevo SMTP - 300 Envíos/día Gratis)
# ------------------------------------------------------------
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=tu_email_registrado_en_brevo@dominio.com
SMTP_PASSWORD=tu_clave_smtp_de_brevo
MAIL_FROM_ADDRESS=tu_email_registrado_en_brevo@dominio.com
MAIL_FROM_NAME="ClientumCRM"

# ------------------------------------------------------------
# 6. WHATSAPP CRM (Meta Cloud API - 1.000 Conversaciones/mes Gratis)
# ------------------------------------------------------------
WHATSAPP_ACCESS_TOKEN=tu_access_token_de_meta
WHATSAPP_PHONE_NUMBER_ID=tu_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=tu_business_account_id
WHATSAPP_WEBHOOK_VERIFY_TOKEN=un_token_inventado_para_verificar_webhook

# ------------------------------------------------------------
# 7. FACTURACIÓN AFIP (Web Services Oficiales - Sin Intermediarios)
# ------------------------------------------------------------
AFIP_CUIT=20123456789
AFIP_ENVIRONMENT=homologacion
AFIP_CERTIFICATE_P12_BASE64=cert_en_base64_obtenido_en_afip
AFIP_PRIVATE_KEY=clave_privada_pem_de_afip

# ------------------------------------------------------------
# 8. PASARELA DE COBROS (Mercado Pago API Directa - $0 Mantenimiento)
# ------------------------------------------------------------
MERCADOPAGO_ACCESS_TOKEN=TEST-1234567890123456-xxxxxx-xxxxxx
MERCADOPAGO_WEBHOOK_SECRET=secreto_para_validar_notificaciones
VITE_MERCADOPAGO_PUBLIC_KEY=TEST-1234567890-public-key
MERCADOPAGO_ENVIRONMENT=sandbox

# ------------------------------------------------------------
# 9. PROTECCIÓN Y SEGURIDAD INTERNA (Plataforma Backend)
# ------------------------------------------------------------
WORKFLOW_ENCRYPTION_KEY=clave_32_caracteres_para_cifrar_workflows
WEBHOOK_SIGNING_SECRET=clave_para_firmar_webhooks_propios
API_KEY_PEPPER=clave_pepper_para_hashear_api_keys_de_usuarios

Instrucciones de Distribución

    Servidor Backend (Render / Koyeb): Copia y pega el bloque completo en la sección Environment Variables / Secrets de tu panel de administración.

    Cliente Frontend (Cloudflare Pages / Vercel): Configura únicamente las variables que comiencen con el prefijo VITE_ (VITE_FIREBASE_* y VITE_MERCADOPAGO_PUBLIC_KEY).

    Restricción de Seguridad: Asegúrate de restringir la VITE_FIREBASE_API_KEY por dominio desde la Consola de Google Cloud / Firebase para evitar accesos no autorizados desde otros orígenes.

