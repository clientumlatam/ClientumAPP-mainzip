You are helping a user configure Clerk DNS for the production domain `clientum.com.ar`.

First determine whether you already know which domain the user is configuring and whether you can directly access or modify DNS for that domain. Do not guess or claim access you do not have.

If you can directly access the DNS provider or registrar and can safely edit DNS on the user's behalf:
- Add or update the exact records below.
- Use the values exactly as written.
- If the provider is Cloudflare or another proxying DNS service, make sure the CNAME records are DNS-only and not proxied.
- If the domain already has a valid DMARC TXT policy, keep it. Clerk accepts existing DMARC policies; the value below is the safe non-enforcing policy recommendation for domains without one.
- After making changes, summarize exactly what you changed.

If you are not sure you can modify DNS directly:
- Determine the DNS provider or registrar by checking the domain's nameservers and registration data.
- Useful commands include: `dig +short NS clientum.com.ar`, `whois clientum.com.ar`, and `nslookup -type=NS clientum.com.ar`.
- If the provider has an API, CLI, or integration you can use, explain exactly what access, credentials, or connection the user needs to provide so you can edit the records.
- Regardless, give the user clear manual steps to add the records through their DNS provider's interface.

Use these exact Clerk DNS records. Do not transcribe values from screenshots.

DNS records:
- Type: CNAME
  Host/Name: accounts
  Full host: accounts.clientum.com.ar
  Target/Value: accounts.clerk.services
  Clerk status: already verified.
- Type: CNAME
  Host/Name: clerk
  Full host: clerk.clientum.com.ar
  Target/Value: frontend-api.clerk.services
  Clerk status: already verified.
- Type: CNAME
  Host/Name: clk._domainkey
  Full host: clk._domainkey.clientum.com.ar
  Target/Value: dkim1.gy4x9xfzqr0z.clerk.services
  Clerk status: already verified.
- Type: CNAME
  Host/Name: clk2._domainkey
  Full host: clk2._domainkey.clientum.com.ar
  Target/Value: dkim2.gy4x9xfzqr0z.clerk.services
  Clerk status: already verified.
- Type: CNAME
  Host/Name: clkmail
  Full host: clkmail.clientum.com.ar
  Target/Value: mail.gy4x9xfzqr0z.clerk.services
  Clerk status: already verified.
- Type: CNAME
  Host/Name: clkmail2
  Full host: clkmail2.clientum.com.ar
  Target/Value: mail2.gy4x9xfzqr0z.clerk.services
  Clerk status: unverified. Clerk did not receive the observed DNS value in the last check.
- Type: CNAME
  Host/Name: pdk1._domainkey.clkmail2
  Full host: pdk1._domainkey.clkmail2.clientum.com.ar
  Target/Value: dkim3.gy4x9xfzqr0z.clerk.services
  Clerk status: unverified. Clerk did not receive the observed DNS value in the last check.
- Type: CNAME
  Host/Name: pdk2._domainkey.clkmail2
  Full host: pdk2._domainkey.clkmail2.clientum.com.ar
  Target/Value: dkim4.gy4x9xfzqr0z.clerk.services
  Clerk status: unverified. Clerk did not receive the observed DNS value in the last check.

When a DNS UI asks for either a relative host or a full hostname, use whichever format that UI expects. The relative host values above are relative to `clientum.com.ar`.

In your response to the user:
- Explicitly say which records are missing.
- Explicitly say which records exist but point to the wrong value.
- Remind the user that the target values must match exactly.
- Tell the user to run Clerk verification again after saving the DNS records.